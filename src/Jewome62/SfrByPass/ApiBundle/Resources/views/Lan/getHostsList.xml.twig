<?xml version="1.0" encoding="UTF-8"?>
<rsp stat="ok" version="1.0">
    {% for decodeur in decodeurs %}
    <host type="stb" name="{{ decodeur.name }}" ip="{{ decodeur.ip }}" mac="{{ decodeur.mac }}" iface="lan1" probe="55" alive="725228" status="online"/>
    {% endfor %}
</rsp>